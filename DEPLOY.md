# 📤 Инструкция по установке на GitHub Pages

## Шаг 1: Создание репозитория

1. Зайдите на https://github.com
2. Нажмите **"New"** (создать репозиторий)
3. Назовите репозиторий, например: `win11-pwa`
4. Сделайте репозиторий **публичным** (Public)
5. Нажмите **"Create repository"**

## Шаг 2: Загрузка файлов

### Вариант A: Через веб-интерфейс
1. В созданном репозитории нажмите **"uploading an existing file"**
2. Перетащите все файлы проекта:
   ```
   index.html
   style.css
   app.js
   apps.js
   manifest.json
   service-worker.js
   README.md
   package.json
   .gitignore
   icons/ (папка со всеми иконками)
   .github/ (папка с workflows)
   ```
3. Нажмите **"Commit changes"**

### Вариант B: Через Git
```bash
cd C:\Users\ТИМ\Desktop\Win11-PWA
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_НИК/win11-pwa.git
git push -u origin main
```

## Шаг 3: Включение GitHub Pages

1. В репозитории перейдите в **Settings** (Настройки)
2. В левом меню выберите **Pages**
3. В разделе **"Build and deployment"**:
   - **Source**: Deploy from a branch
   - **Branch**: main → **/(root)**
4. Нажмите **Save**

## Шаг 4: Проверка

1. Подождите 1-2 минуты
2. Обновите страницу Settings → Pages
3. Вы увидите ссылку вида: `https://ВАШ_НИК.github.io/win11-pwa/`
4. Откройте эту ссылку

## Шаг 5: Проверка PWA

### Откройте страницу диагностики:
```
https://ВАШ_НИК.github.io/win11-pwa/check-pwa.html
```

Она покажет:
- ✅ HTTPS соединение (GitHub Pages предоставляет)
- ✅ Service Worker
- ✅ manifest.json
- ✅ Иконки
- ⚠️ Событие установки (зависит от браузера)

## 📱 Установка на телефон

### Android (Chrome):
1. Откройте `https://ВАШ_НИК.github.io/win11-pwa/`
2. Нажмите меню (⋮) → **"Установить приложение"**
3. Или: **"Добавить на главный экран"**

### iPhone (Safari):
1. Откройте ссылку в **Safari** (не Chrome!)
2. Нажмите **"Поделиться"** (⬆️ внизу экрана)
3. Прокрутите вниз → **"На экран «Домой»"**
4. Нажмите **"Добавить"**

## ⚠️ Возможные проблемы и решения

### Проблема: "Добавить на главный экран" вместо "Установить"
**Это нормально для iOS!** На iPhone/iPad PWA устанавливаются через "На экран «Домой»".

### Проблема: Не работает Service Worker
**Решение:**
1. Проверьте, что service-worker.js загружен (откройте в браузере)
2. Очистите кэш браузера
3. Перезагрузите страницу с Ctrl+Shift+R

### Проблема: manifest.json не найден
**Решение:**
1. Убедитесь, что manifest.json в корне репозитория
2. Проверьте URL: `https://ВАШ_НИК.github.io/win11-pwa/manifest.json`
3. Файл должен открываться в браузере

### Проблема: Иконки не загружаются
**Решение:**
1. Убедитесь, что папка `icons/` загружена со всеми файлами
2. Проверьте пути в manifest.json (должны быть относительными)

### Проблема: После установки не на полный экран
**Решение:**
1. Откройте приложение с домашнего экрана (не из браузера!)
2. На iOS убедитесь, что используете Safari
3. Проверьте `display: "fullscreen"` в manifest.json

## 🔧 Обновление проекта

После изменений в файлах:
```bash
git add .
git commit -m "Описание изменений"
git push
```

GitHub Pages автоматически обновится через 1-2 минуты.

## 📊 Мониторинг

1. **Actions** — статус развёртывания
2. **Pages** — статус сайта
3. **check-pwa.html** — диагностика PWA

## 🎯 Важно для PWA

- ✅ Все файлы должны быть в корне или правильных папках
- ✅ manifest.json должен быть валидным JSON
- ✅ Service Worker должен регистрироваться
- ✅ HTTPS (GitHub Pages предоставляет автоматически)
- ✅ Иконки 192x192 и 512x512 обязательны

## 📞 Если не работает

1. Откройте `check-pwa.html` для диагностики
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все файлы загружены в репозиторий
4. Попробуйте очистить кэш GitHub Pages:
   - Settings → Pages → Save (повторно)

---

**Готово!** 🎉 Ваше приложение доступно по всему миру!
